Prerequisites:

- [Vercel CLI](https://vercel.com/docs/cli) installed globally

To develop locally:

```
pnpm install
pnpm dev
```

```
open http://localhost:4001
```

To build locally:

```
pnpm install
pnpm build
```

To deploy:

```
pnpm install
pnpm deploy
```

{
  "name": "@ephemere/socket",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1 --port 8080 --env-file .env --install-skills false",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "start": "wrangler dev --ip 127.0.0.1 --port 8080 --env-file .env --install-skills false",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@ephemere/db": "workspace:*",
    "@paralleldrive/cuid2": "^2.2.2",
    "drizzle-orm": "^0.45.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@ephemere/typescript-config": "workspace:*",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^20.11.17",
    "@cloudflare/workers-types": "^4.20250601.0",
    "tsx": "^4.7.1",
    "typescript": "^5.8.3",
    "wrangler": "^4.19.1"
  }
}

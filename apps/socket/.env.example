# Server Configuration
PORT=8080

# Database — must match @ephemere/db
DATABASE_URL="postgresql://user:pass@host/dbname?sslmode=require"

# JWT — must match apps/api JWT_SECRET
JWT_SECRET="your_jwt_secret"
